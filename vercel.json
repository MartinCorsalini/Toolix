{
    "version": 2,
    "builds": [
      {
        "src": "angular.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist/prueba"
        }
      }
    ],
    "installCommand": "npm install && npm run build"
  }
  